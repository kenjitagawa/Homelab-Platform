apiVersion: v1
kind: Service
metadata:
  name: example-app
  namespace: example-monitoring-app
  labels:
    app.kubernetes.io/name: example-app
spec:
  selector:
    app.kubernetes.io/name: example-app
  ports:
    - name: web
      port: 8080
      targetPort: web
